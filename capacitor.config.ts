import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'CutreMD5Generator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
